// day!(Day04, Some(0), Some(0));
day!(Day04);

impl Day04 {
  pub fn day(part: Part) -> Answer<u32> {
    // let pairs = Self::INPUT
    //   .lines()
    //   .map(|line| line.split(','))
    //   .map(|pairs| {
    //     pairs
    //       .map(|pair| pair.splitn(1, '-').)
    //       .map(|n| )
    //       .
    //   })
    //   .map(|it| 69)
      // .map(|((a, b), (c, d))| {
      //   (
      //     (a.parse::<i32>().unwrap(), b.parse::<i32>().unwrap()),
      //     (c.parse::<i32>().unwrap(), d.parse::<i32>().unwrap()),
      //   )
      // })
      // .map(|((a, b), (c, d))| (a..=b, c..=d))
      // .filter(|((a, b), (c, d))| (b.max(a) - a.min(a)) <= (d.max(c) - c.min(d)))
      // .count();
      // .filter(|((a_start, a_end), (b_start, b_end))| {
      //   a_start >= b_start && a_end <= b_end || b_start >= a_start && b_end <= a_end
      // })
      // .filter(|((a, b), (c, d))| a.max(b) >= c.min(d) && a.min(b) <= c.max(d))
      // .count();
    // .map(|(a, b)| a.);

    // println!("{pairs:?}");
    let part1 = || 0;
    let part2 = || 0;

    answer!(part, part1, part2)
  }
}
